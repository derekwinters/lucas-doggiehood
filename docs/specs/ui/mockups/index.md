# UI Mockups

HTML mockups for approved wireframes live in this folder, one file per screen
(`<screen>.html`), each corresponding 1:1 with its text spec under
`docs/specs/ui/`. See the [UI Design Process](../../../engineering/ui-design-process.md).

This folder is populated as wireframes are approved (epic #171).
